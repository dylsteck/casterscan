# Required for Neynar user/cast lookups (get from neynar.com)
NEYNAR_API_KEY=""

# Optional — Redis for caching (cache disabled if unset)
REDIS_URL=""

# Optional — CORS allowed origins (comma-separated). Default: casterscan.com, localhost:3000
# ALLOWED_ORIGINS="https://casterscan.com,http://localhost:3000"
