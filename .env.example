# API (apps/api) only — Neynar for user/cast lookups
NEYNAR_API_KEY=""

# API only — Redis for caching (optional; cache disabled if unset)
REDIS_URL=""

# Web (apps/web) only — URL of the API (Vercel: set to your deployed API URL)
API_URL="http://localhost:4000"

# Web only — Axiom logging
NEXT_PUBLIC_AXIOM_DATASET=""
NEXT_PUBLIC_AXIOM_TOKEN=""

# Web only — Google Analytics
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=""